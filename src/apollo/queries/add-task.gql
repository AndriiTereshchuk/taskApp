mutation AddTask($title: String! $description: String!) {
    addTask(title: $title, description: $description) {
        id
        title
        description
        created_at
        updated_at
    }
}